{
  "version": 2,
  "builds": [
    {
      "src": "server/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "server/wsgi.py"
    }
  ],
  "env": {
    "DJANGO_SETTINGS_MODULE": "server.settings",
    "SECRET_KEY": "@secret_key",
    "DEBUG": "False",
    "DATABASE_URL": "@database_url"
  }
}
